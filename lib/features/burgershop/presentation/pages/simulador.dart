import 'package:flutter/material.dart';

class Simulador extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Simular',style: TextStyle(color: Colors.white),),
    );
  }
}
